function Mostrar()
{	var contador;
	contador = 0;
	//alert('iteración while');
	//regla nmotecnica contador siempre inicia en 0, contador < al = de iteraciones 3Paso. sumar el contador.. depues bloque de instrucciones.
	while(contador<10)
	{	contador++;
		document.write("<br>" + contador);

	}
	document.write("<br>sali " + contador);
}//FIN DE LA FUNCIÓN